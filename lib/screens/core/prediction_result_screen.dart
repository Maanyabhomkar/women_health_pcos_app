import 'package:flutter/material.dart';
import 'dart:math';
import 'health_insights_screen.dart';

class PredictionResultScreen extends StatelessWidget {
  final double riskScore;

  PredictionResultScreen({super.key, required this.riskScore});

  String getRiskLabel() {
    if (riskScore < 40) return "Low Risk";
    if (riskScore < 70) return "Moderate Risk";
    return "High Risk";
  }

  Color getRiskColor() {
    if (riskScore < 40) return Colors.green;
    if (riskScore < 70) return Colors.orange;
    return Colors.red;
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,

      appBar: AppBar(
        title: const Text("Your Result"),
        backgroundColor: Colors.white,
        elevation: 0,
        leading: const Icon(Icons.arrow_back),
        actions: const [
          Icon(Icons.share),
          SizedBox(width: 10)
        ],
      ),

      body: SingleChildScrollView(
        padding: const EdgeInsets.all(20),
        child: Column(
          children: [

            const SizedBox(height: 20),

            // 🔥 GAUGE
            SizedBox(
              height: 150,
              child: Stack(
                alignment: Alignment.center,
                children: [
                  CustomPaint(
                    size: const Size(200, 100),
                    painter: GaugePainter(riskScore),
                  ),
                  Column(
                    children: [
                      const Text("Risk Score"),
                      Text(
                        "${riskScore.toInt()}%",
                        style: const TextStyle(
                            fontSize: 28,
                            fontWeight: FontWeight.bold),
                      ),
                      Text(getRiskLabel()),
                    ],
                  )
                ],
              ),
            ),

            const SizedBox(height: 20),

            // 🔹 Description
            const Text(
              "Based on your reported irregular cycles and elevated BMI, there is a moderate likelihood of hormonal imbalance patterns associated with PCOS.",
              textAlign: TextAlign.center,
              style: TextStyle(color: Colors.grey),
            ),

            const SizedBox(height: 20),

            // 🔹 View Insights Button
          GestureDetector(
  onTap: () {
    Navigator.push(
      context,
      MaterialPageRoute(
        builder: (_) => HealthInsightsScreen(
          risk: riskScore,
          cycleLength: 28,
          bmi: 26.4,
          regularity: "irregular",
          symptoms: ["Weight Gain", "Acne"],
        ),
      ),
    );
  },
  child: gradientButton("View Health Insights"),
),

            const SizedBox(height: 10),

            outlineButton("Add to Health Report"),

            const SizedBox(height: 10),

            outlineButton("Ask Health Chatbot"),

            const SizedBox(height: 20),

            const Text(
              "Medical Disclaimer: This result is generated by AI and is not a medical diagnosis.",
              style: TextStyle(fontSize: 11, color: Colors.grey),
              textAlign: TextAlign.center,
            ),
          ],
        ),
      ),
    );
  }

  Widget gradientButton(String text) {
    return Container(
      width: double.infinity,
      height: 55,
      decoration: BoxDecoration(
        gradient: const LinearGradient(
          colors: [Color(0xFFB39DDB), Color(0xFFF48FB1)],
        ),
        borderRadius: BorderRadius.circular(30),
      ),
      child: Center(child: Text(text, style: const TextStyle(color: Colors.white))),
    );
  }

  Widget outlineButton(String text) {
    return Container(
      width: double.infinity,
      height: 55,
      decoration: BoxDecoration(
        borderRadius: BorderRadius.circular(30),
        border: Border.all(color: Colors.grey),
      ),
      child: Center(child: Text(text)),
    );
  }
}

// 🔥 CUSTOM GAUGE
class GaugePainter extends CustomPainter {
  final double value;

  GaugePainter(this.value);

  @override
  void paint(Canvas canvas, Size size) {
    final rect = Rect.fromLTWH(0, 0, size.width, size.height * 2);

    final startAngle = pi;
    final sweepAngle = pi * (value / 100);

    final bgPaint = Paint()
      ..color = Colors.grey.shade300
      ..style = PaintingStyle.stroke
      ..strokeWidth = 12;

    final valuePaint = Paint()
      ..shader = const LinearGradient(
        colors: [Colors.orange, Colors.red],
      ).createShader(rect)
      ..style = PaintingStyle.stroke
      ..strokeWidth = 12;

    canvas.drawArc(rect, pi, pi, false, bgPaint);
    canvas.drawArc(rect, startAngle, sweepAngle, false, valuePaint);
  }

  @override
  bool shouldRepaint(covariant CustomPainter oldDelegate) => true;
}